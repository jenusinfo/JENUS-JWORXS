export interface IUser {
    access_token: string
    token_type: string
    expires_in: number
    userID: string
    firstName: string
    lastName: string
    userName: string
    roles: string
    issued: string
    expires: string
}