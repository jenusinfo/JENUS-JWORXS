import Layout from "components/layouts"
import SettingsPage from "components/pages/Settings"

const Settings = () => {
  return (
    <Layout type="main" pageTitle="Settings">
      <SettingsPage />
    </Layout>
  )
}

export default Settings