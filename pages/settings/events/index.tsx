import Layout from "components/layouts"
import EventsPage from "components/pages/Settings/Events"

const Events = () => {
    return (
        <Layout type="main" pageTitle="Settings - Events">
            <EventsPage />
        </Layout>
    )
}

export default Events