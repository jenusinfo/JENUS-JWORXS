import Layout from "components/layouts"
import UnitsPage from "components/pages/Settings/Units"

const Units = () => {
    return (
        <Layout type="main" pageTitle="Settings - Units">
            <UnitsPage />
        </Layout>
    )
}

export default Units